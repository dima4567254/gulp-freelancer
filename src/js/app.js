import * as fisFunctions from "./modules/functions.js";

fisFunctions.isWebp(); /*flsFunctions*/

import Swiper, {
    Navigation,
    Pagination
} from 'swiper';

const swiper = new Swiper(); /*тут можно через импорт подключать npm install swiper*/ /*для примера*/